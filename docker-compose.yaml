services:

  tech-store-service:
    build:
      context: .
      dockerfile: Dockerfile
    container_name: tech-store-service
    ports:
      - "8080:8080"
    depends_on:
      - database
    environment:
      SERVER_PORT: 8080
      SPRING_DATASOURCE_URL: jdbc:postgresql://techStore-database:5432/postgres
      SPRING_DATASOURCE_USERNAME: postgres
      SPRING_DATASOURCE_PASSWORD: postgres

  database:
    image: postgres:14-alpine
    container_name: techStore-database
    environment:
      POSTGRES_DB: postgres
      POSTGRES_USER: postgres
      POSTGRES_PASSWORD: postgres
    ports:
      - "5439:5432"